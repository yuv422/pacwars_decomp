/*
 * CRITERR.H
 *
 * Public prototypes exported by CRITERR.C (functions that appear in
 * PACWARS.EXE's global symbol table). Reconstructed from PACWARS.TXT.
 */
#ifndef CRITERR_H
#define CRITERR_H

#include "PACWARS.H"

int err_handler(int errval, int ax, int bp, int si);

#endif /* CRITERR_H */
